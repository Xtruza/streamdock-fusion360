{
  "name": "@xtruza/streamdock-fusion360",
  "version": "0.1.0",
  "private": true,
  "description": "StreamDock plugin and optional Autodesk Fusion bridge",
  "scripts": {
    "test": "node --test test/*.test.mjs && node --test com.xtruza.streamdock.fusion360.sdPlugin/plugin/test/*.test.js",
    "validate": "node scripts/validate.mjs",
    "check": "npm run validate && npm test"
  },
  "license": "MIT"
}
